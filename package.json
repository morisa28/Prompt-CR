{
  "name": "codeprompt-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CodePrompt Coach MVP for graded requirement clarification, prompt scoring, prompt generation, and lesson feedback.",
  "scripts": {
    "demo": "node --experimental-strip-types src/cli/index.ts --demo",
    "test": "node --experimental-strip-types tests/run-tests.ts",
    "validate": "python scripts/skill_hub_manager.py validate",
    "stats": "python scripts/skill_hub_manager.py stats"
  },
  "engines": {
    "node": ">=24"
  }
}
